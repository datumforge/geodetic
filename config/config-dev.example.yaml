# server settings
server: 
  debug: true
  dev: true

  # tls settings
  tls:
    enabled: false
  
# db settings
db: 
  debug: true
  driver_name: libsql
  primary_db_source: "file:geodetic.db"
  multi_write: false
